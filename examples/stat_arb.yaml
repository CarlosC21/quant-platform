# Basic stat-arb pair-trading example config

initial_cash: 100000

data_source: examples/data/small_md.csv

strategy:
  params:
    name: stat_arb
    lookback: 20
    entry_z: 1.0
    exit_z: 0.1
    max_position: 500

save:
  directory: null         # CLI overrides this
  save_equity_curve: true
  save_positions: true
